app:
  name: "Oil Gestures"
  mode: "GESTURE_COMMAND"
  debug: true

camera:
  device_id: 0
  width: 1280
  height: 720
  fps: 30
  mirror: true
  # AUTO selects MJPG on Linux/V4L2 and lets other platforms choose natively.
  preferred_fourcc: "AUTO"

runtime:
  show_camera_feed: true
  show_landmarks: true
  show_debug_overlay: true
  safe_exit_key: "q"
  window_name: "Oil Gestures"
