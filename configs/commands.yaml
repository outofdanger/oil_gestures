gesture_command_mode:
  enabled: false

  mapping:
    POINTING_INDEX: "SELECT_OBJECT"
    FIST: "GRAB_OBJECT"
    OPEN_PALM: "RELEASE_OBJECT"
    SQUEEZE: "GRAB_OBJECT"
    RELEASE: "RELEASE_OBJECT"
    ROTATE_CLOCKWISE: "INCREASE_WELL_PRESSURE"
    ROTATE_COUNTERCLOCKWISE: "DECREASE_WELL_PRESSURE"

safety:
  allow_emergency_stop: true
  emergency_stop_gesture: null